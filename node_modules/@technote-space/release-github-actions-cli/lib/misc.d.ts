import type { Config, ContextArgs } from './types';
import type { Context } from '@actions/github/lib/context';
import { GitHelper } from '@technote-space/github-action-helper';
export declare const getRepository: (dir: string) => {
    owner: string;
    repo: string;
} | never;
export declare const getContextArgs: (helper: GitHelper, tagName: string | undefined, branch: string | undefined, dir: string, isTest: boolean | undefined, config: Config) => Promise<ContextArgs>;
export declare const getContext: (args: ContextArgs) => Context;
export declare const getGitHelper: () => GitHelper;
